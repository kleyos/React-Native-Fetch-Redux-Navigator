function goTo(state={}, action){

console.log('state', state);
// let {id} = action;

//   switch (action.type) {
//     case 'GO_TO_SINGLE':
//       const nextIndex = state.index + 1;
      
//       return state.push({
//                           title: 'Single Photo' + id,
//                           index: nextIndex }) 
//     case 'GO_TO_BACK':
//         return  state.index > 0 && state.pop()

//     default:
       return state;
  }

export default goTo;